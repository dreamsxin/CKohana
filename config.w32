// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("kohana", "for kohana support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("kohana", "enable kohana support", "no");

if (PHP_KOHANA != "no") {
	EXTENSION("kohana", "kohana.c");
}

